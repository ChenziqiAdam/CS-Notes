```Python
import matplotlib.pyplot as plt

image, label = train_data[0]
print(f"Image shape: {image.shape}")
plt.imshow(image.squeeze()) # image shape is [1, 28, 28] (colour channels, height, width)
plt.title(label);
```
```Shell
Image shape: torch.Size([1, 28, 28])
```
![[Pasted image 20240707152107.png]]
-   We can turn the image into grayscale using the `cmap` parameter of `plt.imshow()`.
```Python
plt.imshow(image.squeeze(), cmap="gray")
plt.title(class_names[label]);
```
![[Pasted image 20240707152132.png]]
- In essence, what we are going to do is taking pixel values and building a model to find patterns in them to use on future pixel values.


#CS/Courses/PyTorch 
>[[L5.3 prepare the dataset]]