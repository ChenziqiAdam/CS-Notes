- If you've trained a model on a certain dataset, chances are you'd like to make a prediction on on your own custom data. In our case, since we've trained a model on pizza, steak and sushi images, how could we use our model to make a prediction on one of our own images?
- To do so, we can load an image and then **preprocess it in a way that matches the type of data our model was trained on**.
	- In other words, we'll have to convert our own custom image to a tensor and make sure it's in the right datatype before passing it to our model.
- [[L6.12.0 get an image]]
- [[L6.12.1 load in a custom image with PyTorch]]
- [[L6.12.2 predict on custom images with a trained PyTorch model]]
- [[L6.12.3 functionize custom image prediction together]]
![[Pasted image 20240731200245.png]]

#CS/PyTorch/dataset/custom-dataset 


#CS/Courses/PyTorch 
>[[L6. PyTorch Custom Dataset]]