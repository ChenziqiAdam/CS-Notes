- Since we've got the results of `model_1` saved in a results dictionary, `model_1_results`, we can plot them using `plot_loss_curves()`.
```Python
plot_loss_curves(model_1_results)
```
![[Pasted image 20240731134940.png]]
- These don't look very good either. Maybe this is due to that our model is not overfitting and data augmentation is a way to help with overfitting.


#CS/Courses/PyTorch 
>[[L6.10 model_1-TinyVGG with Data Augmentation]]