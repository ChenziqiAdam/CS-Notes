- Since we're working on a multi-class classification problem, we'll use the `nn.CrossEntropyLoss()` method as our loss function.
- And we'll stick with using SGD with a learning rate of 0.1 for optimizing our `model_4` parameters.
	```Python
	# Create loss and optimizer
	loss_fn = nn.CrossEntropyLoss()
	optimizer = torch.optim.SGD(model_4.parameters(), 
	                            lr=0.1)
```
- More see the [PyTorch documentation](https://pytorch.org/docs/stable/nn.html#loss-functions).
![[Pasted image 20240702202552.png]]

#CS/PyTorch/Classification/model #CS/PyTorch/Classification/model/optimizer/SGD  #CS/PyTorch/Classification/model/loss-function/CrossEntropyLoss 


#CS/Courses/PyTorch 
>[[L4.3 put it all together with a multi-class classification problem]]