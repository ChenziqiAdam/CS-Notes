- Data can be anything.
- [[L3.2.1 machine learning diagram]]
- Example: linear regression (a straight line): y=a+bx
	```Python
	# create known parameters
	weight = 0.7
	bias = 0.3
	
	# create
	start = 0
	end = 1
	step = 0.02
	X = torch.arange(start, end, step).unsqueeze(dim=1)
	
	y = weight * X + bias
```
- Relationship between `X` (**features**) and `y` (**labels**): [[L3.2.2 three data sets]]

#CS/PyTorch/Regression 


#CS/Courses/PyTorch
>[[L3. PyTorch workflow fundamentals]]