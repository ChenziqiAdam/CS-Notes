- Example code:
	```Python
	model_0.eval()
	
	with torch.inference_mode():
		# 1. Do the forward pass
		test_pred = model_0(X_test)
		
		# 2. Calculate the loss
		test_loss = loss_fn(test_pred, y_test)
		
		# Print out what is happening
	if epoch % 10 == 0:
		print(f"Epoch: (epoch) | Loss: (loss) I Test loss: (test_loss)")
		print(model_0.state_dict())
```
	- `model_0.eval()`: turn off unnecessary settings in the moel for evaluation/testing (dropout/batch norm layers)
	- `with torch.inference_mode():`: turn off gradient tracking and more behind the scnes

#CS/PyTorch/model/testing-loop 


#CS/Courses/PyTorch 
>[[L3.4.2 build an optimization loop]]