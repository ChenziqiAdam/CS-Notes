- To see when pass the test data `X_test` it is how closely predicts `y_test`, use `torch.inference_mode()`
	- When we pass data to our model, it'll go through the model's `forward()` method and produce a result using the computation we've defined.
	```Python
	# Make predictions with model
	with torch.inference_mode(): 
	    y_preds = model_0(X_test)
```
	-  [`torch.inference_mode()`](https://pytorch.org/docs/stable/generated/torch.inference_mode.html) is used as a [context manager](https://realpython.com/python-with-statement/) (that's what the `with torch.inference_mode():` is) to make the predictions.
		- `torch.inference_mode()` turns off a bunch of things (like gradient tracking, which is necessary for training but not for inference) to make **forward-passes** (data going through the `forward()` method) faster.
		- Compared to old version `torch.no_grad()`, although they are similar, `torch.inference_mode()` is newer, potentially faster and preferred. 
			- See this [Tweet from PyTorch](https://twitter.com/PyTorch/status/1437838231505096708?s=20) for more.
- Visualize with `plot_predictions(predictions=y_preds)`
- Though the prediction here may be bad due to the initialization of random parameters, next we will train the model.

#CS/PyTorch/model 


#CS/Courses/PyTorch 
>[[L3. PyTorch workflow fundamentals]]