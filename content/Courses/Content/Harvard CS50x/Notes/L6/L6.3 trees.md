- List: advantage is dynamic, while disadvantage is out-of order
	- Then comes **Trees**.
		```MarkDown
		    1
	       / \
	      2   3
	     / \ / \
	    4  5 6  7
	```
	- This means **binary search** is appliable, but may takes up a huge amount of space.
	```C
	bool search(node *tree, int number) {
		if (tree == NULL) {
			return false;
		}
		else if (number < tree -> number) {
			return search(tree -> left, number);
		}
		else {
			return search(tree -> right, number);
		}
	}
```
	- **BST (Binary Search Tree) always needs to balance.**

#CS/C/tree


#CS/Courses/CS50x 
>[[L6. Data Structures]]