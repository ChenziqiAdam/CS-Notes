- **Common events that browser can listen for**:
	- `blur`
	- `change`
	- `click`
	- `drag`
	- `focus`
	- `keyup`
	- `load`
	- `mousedown`
	- `mouseover`
	- `mouseup`
	- `submit`
	- `touchmove`
	- `unload`
- Example: geolocation(定位)
	```HTML
	<!DOCTYPE html>
		
		<html lang="en">
			<head>
				<title>geolocation</title>
			</head>
			<body>
				<script>
				
					navigator.geolocaiton.getCurrentPosition(function(position) {
						document.write(posion.coords.latitude + ", " + position.coords.longitude);
					});
				
				</script>
		    </body>
	    </html>
```

#CSS/HTML/event


#CS/Courses/CS50x 
>[[L9.4 JavaScript]]