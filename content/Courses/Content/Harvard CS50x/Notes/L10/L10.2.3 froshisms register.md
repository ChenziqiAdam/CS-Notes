- Advanced Example: froshims register
	```HTML
	<!-- layout.html -->
	<!DOCTYPE html>
			
	<html lang="en">
		<head>
			<meta name="viewport" content="initial-scale=1, width=device-width">
			<title>register</title>
		</head>
		<body>
			 {% block body %}{% endblock %}
		</body>
	</html>
```
	```HTML
	<!-- indext.html -->
	{% extends "layout.html" %}
			
	{% block body %}
				
		<h1>Register</h1>
	    <form action="/register" method="post">
	        <input autocomplete="off" autofocus name="name" placeholder="Name" required type="text">
	        <select name="sport">
		        <!-- Jinja has similar syntax with Python -->
	            <option disabled selected value="">Sport</option>
	            {% for sport in sports %}
	                <option value="{{ sport }}">{{ sport }}</option>
	            {% endfor %}
	        </select>
	        <button type="submit">Register</button>
	    </form>
					
	{% endblock %}
```
	```Python
	# app.py
	from flask import Flask, render_template, request
	
	app = Flask(__name__)
	
	SPORTS = ["Basketball", "Soccer", "Ultimate Frisbee"]
			
	@app.route("/")
	def index():
		return render_template("index.html", sports=SPORTS)
	
	@app.route("/register", methods=["POST"])
	def register():
		if not request.form.get("name") or not request.form.get("sport") not in SPORTS:
			return render_template("failure.html")
		return render_template("success.html") # No need for else
```
	```HTML
	<!-- success.html -->
	{% extends "layout.html" %}
				
	{% block body %}
				
		<h1>Success!</h1>
	    <p>You have successfully registered for {{ sport }}.</p>
					
	{% endblock %}
	
	<!-- failure.html -->
	{% extends "layout.html" %}
			
	{% block body %}
				
	    <h1>You are not registered!</h1>
					
	{% endblock %}
```
	- [[L10.2.3.1 other forms of register]]
	- Better froshims: more robust validation, i.e. [[L10.2.3.2 customed error]]
	- More better froshims: store data with [[L10.2.3.3 SQL]]


#CS/Courses/CS50x 
>[[L10.2 Flask basic]]