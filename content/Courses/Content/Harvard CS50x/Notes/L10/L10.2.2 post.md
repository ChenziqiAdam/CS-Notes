- URL: `https://www.example.com/route?key=value&key=value`. What if the values here are some private information?
	`method="post"` then there will be no exposed value
	- Example: `post`
		```HTML
		<form action="/greet" method="get">
			<input autocomplete="off" autofocus name="name" placeholder="Name" type="text">
			<button tyoe="submit">Greet</button>
		</form>
```
		- Remember to modify `app.py` s.t. it will support `post`. (`get` is method by default.)
		```Python
		@app.route("/greet", methods=["POST"])
		def greet():
			name = request.form.get("name", "world")
		    return render_template("greet.html", name=name)
```
		- `get` <-> `request.args`
		- `post` <-> `request.form`
	- Example: combine `post` and `get`
	```Python
		@app.route("/", methods=["GET", "POST"])
		def index():
			if request.method == "POST":
				name = request.form.get("name", "world")
			    return render_template("greet.html", name=name)
			return render_template("index.html")
```
	```HTML
			<!-- index.html -->
			{% extends "layout.html" %}
			
			{% block body %}
			
				<form action="/" method="post">
			        <input autocomplete="off" autofocus name="name" placeholder="Name" type="text">
			        <button tyoe="submit">Greet</button>
			    </form>
				
			{% endblock %}
```
	```HTML
			<!-- greet.html -->
			{% extends "layout.html" %}
			
			{% block body %}
			
				<!-- judge whether name is a empty string -->
				<!-- if is, name is "world" by default -->
				hello, {% if name %}{{ name }}{% else %}world{% endif %}
				
			{% endblock %}
```

#CS/HTML/post


#CS/Courses/CS50x 
>[[L10.2 Flask basic]]