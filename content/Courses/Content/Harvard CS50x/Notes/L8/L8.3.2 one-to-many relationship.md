- One show may correspond to many genres.
	- e.g. 
		```SQL
		SELECT title FROM shows WHERE id IN (
		SELECT show_id FROM genres WHERE genre = 'Comedy' LIMIT 10);
			
		SELECT genre FROM geres WHERE show_id = (
		SELECT id FROM shows WHERE title = 'Game of Thrones');
			
		SELECT title, genre FROM shows 
		JOIN genres ON shows.id = genres.show_id WHERE id  = 63881;
	```


#CS/Courses/CS50x 
>[[L8.3 SQL Relational Database]]