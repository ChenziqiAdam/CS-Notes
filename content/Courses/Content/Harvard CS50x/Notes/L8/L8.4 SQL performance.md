- Record the time: `.timer ON`
- Indexes query: `CREATE INDEX name ON table (column, ...);`
	build some data structures manually so that the query of a specific column will become faster
	- only need to build once, but need to maintain due to `UPDATE` and `DELETE`
	- Essentially it builds a B-tree, i.e. constant time.
	- However, such index comes at a cost of space. Also requires maintenance due to `UPDATE` and `DELETE`.

#CS/SQL/index


#CS/Courses/CS50x 
>[[L8. SQL]]