- **CLA**
	- need to import `sys`
	- Example:
	```Python
	from sys import argv
	
	if len(argv) == 2:
		print(f"Hello, {agrv[1]}!")
	else:
		print("Hello, world!")

	# Multiple ways to iterate over all CLA
	for i in range(len(argv)):
		print(argv[i])
	for arg in argv:
		print(arg)
	for arg in argv[0:]:
		print(arg)
```
	- `[0:]`: slicing operation that selects all elements from index 0 to the end of the list


#CS/Courses/CS50x 
>[[L7. Python]]