- `list`:
	```Python
	scores = []
	for _ in range(3):
		score = int(input("Score: "))
		scores.append(score) # scores += [score]
	average = sum(scores) / len(scores)
```


#CS/Courses/CS50x 
>[[L7. Python]]