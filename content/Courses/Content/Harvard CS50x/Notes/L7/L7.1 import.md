- `import` + library
	```Python
	name = input("What's your name? ")
	print("hello,  ", name)
	print("hello, " + name)
	print(f"hello, {name}")
```
	- `input()`: return a string
		- Q. what if users want an integer?
			- **Cast**
	- `f`: ***fstring*** i.e. **formal string**
	- `" "` and `' '` are equal in Python.


#CS/Courses/CS50x 
>[[L7. Python]]