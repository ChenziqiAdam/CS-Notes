- Q. what about search in Python?
	- Example: **linear search**
		```Python
		import sys
	
		names = ["Hermione", "Harry", "Ron", "Draco"]
		name = input("Name: ")
		for n in names:
			if name == n:
				print("Found!")
		# if name in names:
				sys.exit(0)
		print("Not found!")
		sys.exit(1)
```


#CS/Courses/CS50x 
>[[L7. Python]]