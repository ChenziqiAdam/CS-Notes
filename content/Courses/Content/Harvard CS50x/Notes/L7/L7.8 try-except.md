- Q. what if the user is asked to input an integer but he does not cooperate?
	```Python
	def get_n():
		while True:
			try:
				n = int(input("Enter an integer: "))
				if n > 0:
					return n # Simultaneously break the loop and the function
			except ValueError:
				print("Not an integer!")
```


#CS/Courses/CS50x 
>[[L7. Python]]