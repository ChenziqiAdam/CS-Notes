- ***Integer Overflow***
	- result of infinite bits
	- replace `int` with `long`
- ***Truncation***
	- Casting: add `(float)` before the variable name
	- replace `int` with `float`
- ***Float-point Imprecision***
	- replace `float` with `double`
	- `printf("%.20f", n)` i.e. output to 20 decimal points

#CS/C/integer-overflow #CS/C/truncation #CS/C/floatpoint-imprecision


#CS/Courses/CS50x 
>[[L2. C]]