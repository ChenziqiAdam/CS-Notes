- `i + 1 = i`  
	`i += 1` 
	`i++`
- `if (`condition1`) {`
	function1
	`}`
	`else if (`condition2`) {`
	function2
	`}`
	`else {`
	function3
	`}`
- `while (`condition`) {`
	function
	`}
	- **Must update s.t. the condition can be reached and the while loop ends
	- <u>stop the execution</u>: `ctrl + C`
	- The while loop executes at least once, i.e. the first iteration runs without checking the condition.
	- `while (true)`
		`while (1)`
		i.e. keep checking some conditions in games
- `for (int i = 0; i < n; i++) {`
	function
	`}
	- Must have three parts with two semicolons in the first line
	- Defensive coding: `const int n = 3`
		cannot change the variable elsewhere
- `do {`
	function
	`}
	`while (`condition`) 


#CS/Courses/CS50x 
>[[L2. C]]