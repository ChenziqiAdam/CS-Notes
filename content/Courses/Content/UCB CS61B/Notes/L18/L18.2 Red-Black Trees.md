- There are many types of search trees:
	- Binary search trees: Can balance using rotation, but we have no algorithm for doing so (yet).
	- 2-3 trees: Balanced by construction, i.e. no rotations required.
- Q. how to build a BST that is structurally identical to a 2-3 tree?
	- Since 2-3 trees are balanced, so will our special BSTs.
	- Because a 2-3 tree with only 2-nodes is exactly the same as BST, we will focus on 2-3 tree with 3-nodes.
- Representing a 2-3 Tree as a BST: Dealing with 3-nodes
	- Possibility 1: Create dummy “glue” nodes.
		![[Pasted image 20240906094520.png]]
		- Inelegant and ugly code.
	- Possibility 2: Create “glue” links with the smaller item <u>off to the left</u>.
		![[Pasted image 20240906094703.png]]
		- Idea is commonly used in practice (e.g. `java.util.TreeSet`).
- ***Left-Leaning Red Black Binary Search Tree*** (***LLRB***)
	A BST with left glue links that represents a 2-3 tree is often called a “Left Leaning Red Black Binary Search Tree” or ***LLRB***.
	- **LLRBs are normal BSTs!** 
	- There is a **1-1 correspondence** between an LLRB and an equivalent 2-3 tree.
	- The red is just a convenient fiction. Red links don’t “do” anything special.
	![[Pasted image 20240906095109.png]]
	- Valid LLRBs
		![[Pasted image 20240906100218.png]]
	- LLRBs Height
		- Suppose we have a 2-3 tree of height H, then the maximum height of the corresponding LLRB is:
			H (black) + H + 1 (red) = 2H + 1
- Left-Leaning Red Black Binary Search Tree (LLRB) Properties:
	- No node has two red links (otherwise it’d be analogous to a 4 node, which are disallowed in 2-3 trees).
	- Every path from root to a leaf ("a null reference" is better) has the same number of <u>black links</u> (because 2-3 trees have the same number of links to every leaf). LLRBs are therefore balanced.

#CS/Data-Structure/Red-Black-Trees #CS/Data-Structure/LLRBs 


#CS/Courses/CS61b 
>[[L18. Red Black Trees]]