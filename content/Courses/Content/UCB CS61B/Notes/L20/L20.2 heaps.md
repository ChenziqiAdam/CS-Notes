- BSTs would work, but need to be kept bushy and duplicates are awkward.
- Binary min-heap: Binary tree that is **complete** and obeys **min-heap property**.
	- **Min-heap**: Every node is less than or equal to both of its children.
	- **Complete**: Missing items only at the bottom level (if any), all nodes are as far left as possible.
	![[Pasted image 20240907105801.png]]
- Heaps lend themselves very naturally to implementation of a priority queue.
	1. `getSmallest()`: return the root node
	2. `add()`:
		1. Add to the end of the heap temporarily
		2. Swim up the hierarchy to the rightful place
	3. `removeSmallest()`:
		1. Swap the last item in the heap into the root (and delete the previous root)
		2. Then demote repeatedly, always taking the ‘better’ successor.

#CS/Data-Structure/heaps


#CS/Courses/CS61b 
>[[L20. Heaps and PQs]]