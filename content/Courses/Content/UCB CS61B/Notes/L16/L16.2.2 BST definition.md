- *Tree*
	A tree consists of:
	- A set of nodes.
	- A set of edges that connect those nodes.
		- **Constraint: There is exactly one path between any two nodes**
		![[Pasted image 20240905100432.png]]
- *Rooted Tree*s and *Rooted Binary Trees*
	In a rooted tree, we call one node the root.
	- Every node N except the root has exactly one parent, defined as the first node on the path from N to the root.
	- Unlike [(most) real trees](https://www.amusingplanet.com/2010/12/baobab-upside-down-tree.html), the root is usually depicted at the top of the tree.
	- A node with no child is called a leaf.
	- In a rooted binary tree, every node has either 0, 1, or 2 children (subtrees).
	![[Pasted image 20240905100555.png]]
- *Binary Search Trees*
	- A binary search tree is a rooted binary tree with the BST property. 
	- *BST* Property: for every node X in the tree:
		- Every key in the left subtree is less than X’s key.
		- Every key in the right subtree is greater than X’s key.
	- Ordering must be complete, transitive, and antisymmetric. Given keys p and q:
		- Exactly one of p ≺ q and q ≺ p are true. 
		- p ≺ q and q ≺ r imply p ≺ r.
    - One consequence of these rules: No duplicate keys allowed!
		- Keeps things simple. Most real world implementations follow this rule.
		![[Pasted image 20240905100830.png]]

#CS/Data-Structure/trees #CS/Data-Structure/BSTs 


#CS/Courses/CS61b 
>[[L16.2 Binary Search Tree (BSTs)]]