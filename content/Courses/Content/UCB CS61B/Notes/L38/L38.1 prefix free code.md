- Increasing Optimality(最优性) of Coding
	- By default, English text is usually represented by sequences of characters, each 8 bits long
		- e.g. ‘d’ is 01100100 (binary), 64 (hexadecimal)
- Easy way to compress: Use fewer than 8 bits for each letter.
	- Have to decide which bit sequences should go with which letters.
	- More generally, we’d say which *codewords* go with which *symbols*.
- Prefix Free Codes
	![[Pasted image 20241030201004.png]]
	![[Pasted image 20241030201039.png]]
	- This means any code cannot from tries.
- A prefix-free code is one in which no codeword is a prefix of any other.
	- Different prefix-free codes are better for different texts (binary or hexadecimal suits different cases).
- It’d be useful to have a procedure that calculates the “best” code for a given text.

#CS/Compression/prefix 


#CS/Courses/CS61b 
>[[L38. Compression]]