- There are many ways to implement an abstract data type.
	![[Drawing 2024-09-08 10.22.26.excalidraw]]
- Runtimes for our Balanced Search Tree and Hash Table implementations were very fast. 
	If we know that our keys all have some common special property, we can sometimes get even better implementations.
	- Example #1: Suppose we know our keys are always single ASCII characters.
		- Can just use an array! Simple and fast.
	- Example #2: Suppose we know that our keys are always strings.
		- Can use a special data structure known as a ***Trie***.
		- Basic idea: Store each letter of the string as a node in a tree.
- Suppose we have a set containing “sam”, “sad”, “sap”, “same”, “a”, and “awls”.
	- Below, we see the BST and Hash Table representation.
		![[Pasted image 20240919090253.png]]
- For String keys, we can use a ***Trie*** (short for **Retrieval Tree**). Key ideas:
	- Every node stores only one letter.
	- Nodes can be shared by multiple keys.
	- How to make it clear that our set contains “sam”, “sad”, “sap”, “same”, “a”, and “awls”, but not “aw”, “awl”, “sa”, etc.
		- Search Hits and Misses
		![[Pasted image 20240919090657.png]]

#CS/Data-Structure/tries


#CS/Courses/CS61b 
>[[L26. Prefix Operations and Tries]]