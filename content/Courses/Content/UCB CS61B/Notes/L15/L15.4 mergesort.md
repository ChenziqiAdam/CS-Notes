- Selection Sort: a prelude(前奏) to Mergesort
	- Find the smallest unfixed item, move it to the front, and ‘fix’ it.
	- Sort the remaining unfixed items using selection sort.
	- R(N) = Θ(N<sup>2</sup>)
- Merge Operation: another prelude to Mergesort
	- Given two sorted arrays, the merge operation combines them into a single sorted array by successively copying the smallest item from the two arrays into a target array.
	![[Pasted image 20240905091140.png]]
	- R(N) = Θ(N)
- Idea: Use merge to speed up sorting process
	- One merge layer
		![[Pasted image 20240905091433.png]]
	- Two merge layer
		![[Pasted image 20240905091528.png]]
	- We can do better by adding more merge layers. What happens if there is no selection sort at the end? Mergesort!
		- ==Game-changing!== Completely different from selection sort.
		![[Pasted image 20240905091624.png]]
	- Mergesort has worst case runtime = Θ(N log N).
		- Every level takes ~N AU.
			- Top level takes ~N AU.
			- Next level takes ~N/2 + ~N/2 = ~N.
			- One more level down: ~N/4 + ~N/4 + ~N/4 + ~N/4 = ~N.
		- Thus, total runtime is ~Nk, where k is the number of levels.
			- How many levels? Goes until we get to size 1.
			- k = log2(N).
		- Overall runtime is Θ(N log N).

#CS/Algorithm/sort/selection-sort #CS/Algorithm/sort/merge-sort  


#CS/Courses/CS61b 
>[[L15. Asymptotics II]]