- Use `String.join()` is much more efficient.
	- Pre-condition: it requires an **iterable** object.
```Java
@Override
public String toString() {
	List<String> listOfItems = new ArrayList<>();
	for (T x : this) {
		listOfItems.add(x.toString());
	}
	return "{" + String.join(delimiter.",", listOfItems) + "}";
}
```
- `.of()` method
```Java
public static <Glerp> ArratSet<Glerp> of(Glerp... stuff) {
	ArraySet<Glerp> returnSet = new ArraySet<Glerp>();
	for (Glerp x : stuff) {
		returnSet.add(x);
	}
	return returnSet;
}
```

#CS/Java/toString #CS/Java/of


#CS/Courses/CS61b 
>[[L11. Exceptions, Iterators, Object Methods]]