- Java allows us to iterate through Lists and Sets using a convenient shorthand syntax sometimes called the "foreach" or "enhanced for" loop.
	- e.g. `for (int i : aset) {...}`
	- p.s. This actually creates a copy of the original item, i.e. it doesn't make changes to the original one.
- Q. how iteration works?
	- An alternate, uglier way to iterate through a List is to use the `iterator()` method.
	```Java
	// Ugly iteration
	Set<Integer> javaset = new HashSet<Integer>();
	...
	Iterator<Integer> seer = javaset.iterator();
	
	while (seer.hasNext()) {
		System.out.println(seer.next());
	}
```
	- To support the ugly iteration for `ArraySet`:
		- Add an `iterator()` method to `ArraySet` that returns an `Iterator<T>`
		- The `Iterator<T>` that we return should have a useful `hasNext()` and `next()` method
	```Java
	public interface Iterator<T> {
		boolean hasNext();
		T next();
	}
```
	```Java
	Iterator<Integer> aseer = aset.iterator();
	
	while (aseer.hasNext()) {
		System.out.println(seer.next());
	}
```
	- Btw, thos is how Sets works as well (i.e. the *Iterable Interface*).
- Iteration Summary
	To support the enhanced for loop:
	- Add an `iterator()` method to your class that returns an `Iterator<T>`
	- The `Iterator<T>` returned should have a useful `hasNext()` and `nest()` method.
	- Add `implements Iterabl<T>` to the line defining your class.

#CS/Java/iterator


#CS/Courses/CS61b 
>[[L11. exceptions, iterators, object methods]]