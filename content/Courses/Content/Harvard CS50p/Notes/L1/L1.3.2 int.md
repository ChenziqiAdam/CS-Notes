- **int**: integer
	- [[L1.3.2.1 Interactive Mode]]
	- **For `input` in python, the default variable type is `string`.
		- can cast to other variable types
		```Python
		x = int(input("What's x? "))
		y = int(input("What's y? "))
		print(x + y)
```
		- **Corner Case**
			- what it user does not type in an integer?
	- **Integer in Python can be as big as you want.**

#CS/Courses/CS50p
>CS50p L1.3 Variables