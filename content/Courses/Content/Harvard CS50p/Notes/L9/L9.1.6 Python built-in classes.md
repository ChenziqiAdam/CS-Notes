- Many data types in Python are underneath classes.
	- `int` is actually a class.
		`class int(x, base=1)
	- `str` is also a class
		`class str(object='')`
	- `list` is a class too
		`class list([literable])`
	- `type()` enable to show the type of data types.


#CS/Courses/CS50p
>[[L9.1 OOP and Instance Variables & Methods]]