- ***Operator Overloading (运算符重载)***: operators mean more than one meaning
	`+`: addition, concatenation for `str` and `list`
	- Example: vault(金库)
		```Python
		class Vault:
			def __init__(self, galleons=0, sickles=0, knuts=0):
				self.galleons = galleons
				self.sickles = sickles
				self.knuts = knuts
				
			def __str__(self):
				return f"{self.galleons} Galleons, {self.sickles} Sickles, {self.knuts} Knuts"
				
			def __add__(self, other): # overload addition
				galleons = self.galleons + other.galleons
				sickles = self.sickles + other.sickles
				knuts = self.knuts + other.knuts
				return Vault(gaelleons, sickles, knuts)
```
		- `def __add__(self, other)`: the `other` here can be any objects as long as the `self` class has overloaded addition.
- Python does not support inventing new operator by programmers.

#Python/operator-overloading 


#CS/Courses/CS50p
>[[L9. Object-Oriented Programming]]