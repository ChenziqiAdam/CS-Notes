- ***lambda function***
	```Python
	# The same code as above
	for student in sorted(students, key=lambda student: student["name"]):
		print(f"{student['name']} is in {student['house']}")
```
	- ***lambda function***: `lambda arguments: expression`
		function that is only used at once and not used any more
	- `lambda student: student["name"]`:
		- `student` after `lambda` is the parameter the funciton takes in
		- content after `:` is the body of the function
	
#Python/lambda-function 


#CS/Courses/CS50p
>[[L7.3 File Output (.csv)]]