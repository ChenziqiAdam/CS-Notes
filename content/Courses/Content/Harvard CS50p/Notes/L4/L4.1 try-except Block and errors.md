- ***Try-Except* Block**
	- `try`
	- `except`
	- `else`
- `SyntaxError`: up to the programmer to solve in the code
- `ValueError`:
	```Python
	try:
		x = int(input("What is x "))
		print(f"x is {x}")
	except ValueError:
		print("x is not an integer")
	# print(f"x is {x}")
```
- `NameError`:
	- if add `print(f"x is {x}")` below the code above
		- once the user types in some non-int i.e. "cat", there will happen `NameError`.
		- `input()` works fine, but there is error when it comes to `int()`. So the error interrupts the value assignment process.
		- there is no value passed to `x`
	- Solution:
		```Python
		try:
			x = int(input("What is x "))
		except ValueError:
			print("x is not an integer")
		else: # if exception does not happen, continue
			print(f"x is {x}")
	
		while True:
			try:
				x = int(input("What is x "))
				break
			except ValueError:
				print("x is not an integer")
			# else: break
		print(f"x is {x}")
	
		def get_int():
			while True:
				try:
					x = int(input("What is x "))
					return n # Simultaneously break the loop and the function
				except ValueError:
					pass # Just ask users "What is x " without keeping reminding them "x is not an integer"
```


#CS/Courses/CS50p
>[[L4. Exceptions]]