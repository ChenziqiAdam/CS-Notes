### 1. Regexes and `re` Library
- ***Regexes***: **Regular Expression (正则表达式)**
	- define patterns in order to validate input
- `re` library for regexes
	- `re.search(pattern, string, flags=0)`
	- `re.match(pattern, string, flags=0)`
	- `re.split(pattern, string, maxsplit=0, flags=0)`
	- `re.findall(pattern, string, flags=0)`
### 2. `re.search()`
[[L8.1 re.search()]]
### 3. Format data
[[L8.2 Format data]]
### 4. Extract data
[[L8.3 Extract data]]

#CS/Python/regexes 


#CS/Courses/CS50p
>[[Harvard CS50p]]