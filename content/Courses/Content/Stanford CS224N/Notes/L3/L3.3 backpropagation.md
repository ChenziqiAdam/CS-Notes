- **Computation Graphs** (**CG**) and **Forward Propagation**
	![[Pasted image 20250927092907.png]]
- ***Backpropagation***
	- go backwards along edges
	- pass along gradients
	![[Pasted image 20250927092934.png]]
	- Single Node Example
		- Each node has a *local gradient*, i.e. the gradient of its output w.r.t its input
		- *downstream gradient* = *upstream gradient* x *local gradient*
		![[Pasted image 20250927093242.png]]
	- Example
		![[Pasted image 20250927160620.png]]
		- + “distributes” the upstream gradient 
		- max “routes” the upstream gradient 
		- * “switches” the upstream gradient
	- Efficiency: Compute all gradients at once
		- Compute all the gradients at once 
		- Analogous to using $\delta$ when we computed gradients by hand
- General Back-Prop in CG
	1. *Fprop*: Visit nodes in topological sort order
		- Compute value of node given predecessors
	2. *Bprop*:
		- Initialize output gradient = 1
		- Visit nodes in reverse order:
			- Compute gradient with respect to each node using gradient with respect to successors
			- Given $\{y_1, y_2, \ldots, y_n\} = \text{successors of } x$
				$\frac{\partial z}{\partial x} = \sum_{i=1}^n \frac{\partial z}{\partial y_i} \frac{\partial y_i}{\partial x}$
		- Done correctly, big *O()* complexity of fprop and bprop is the same.
- Automatic Differentiation
	- The gradient computation can be automatically inferred from the symbolic expression of the *fprop*
	- Each node type needs to know how to compute its output and how to compute the gradient wrt its inputs given the gradient wrt its output
	- Modern DL frameworks (e.g. PyTorch) do *bprop* for you but mainly leave layer/node writer to hand-calculate the local derivative


#CS/ML/neural-network/backpropagation 


#CS/Courses/CS224N 
> [[L3. Neural Net Learning and Backpropagation]]