- Modern neural networks (esp. language models) are enormous. But building large neural networks wasn’t easy or obvious at first. It took a long time and much work to make deep neural networks practical!

# Regularization

- A full loss function includes **regularization** over all parameters $\theta$, e.g., L2 regularization:
$$
  J(\theta) = \frac{1}{N} \sum_{i=1}^N -\log \left( \frac{e^{f_{y_i}}}{\sum_{c=1}^C e^{f_c}} \right) + \lambda \sum_k \theta_k^2
	$$
- **Classic view:** Regularization works to prevent **overfitting** when we have a lot of features (or later a very powerful/deep model, etc.)
- **Now:** Regularization **produces models that generalize well** when we have a "big" model
	- We do not care that our models overfit on the training data, even though they are **hugely overfit**
	![[Pasted image 20251017154107.png]]
- **Dropout**
	- Algorithm
		- During training: For each data point each time randomly set input to 0 with probability $p$ "dropout ratio" via dropout mask
		- During testing: Multiply all weights by $1-p$
			- No other dropout
		![[Pasted image 20251017154513.png]]
	- Why?
		- In this way, the model has to take full advantage of all parameters and do not overly rely on one single parameter.
		- Prevents Feature Co-adaptation = Good Regularization! Use it everywhere!

# Vectorization

- Never for loops. Matrices are awesome!

# Parameter Initialization

- Must initialize weights to small random values (i.e., not zero matrices!)
	- To avoid symmetries that prevent learning/specialization.
	- Initialize all other weights $\sim \text{Uniform}(–r, r)$, with $r$ chosen so numbers get neither too big or too small.
	- With advanced layer normalization, this is less important.

# Optimization

- SGD works just fine, but getting good results will often require hand-tuning the learning rate.
- More sophisticated “adaptive” optimizers that scale the adjustment to individual parameters by an accumulated gradient
	- Adam
	- AdamW
	- NAdamW
	- ...


#CS/Courses/CS224N 
>[[L5. Recurrent Neural Networks]]