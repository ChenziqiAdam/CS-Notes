- **Query plan** (a.k.a. dataflow graph)
	- Edges encode "flow" of tuples
	- Vertices = Relational Algebra Operators
	- Source vertices = table access operators ...
- **Query Executor** Instantiates **operators**
	- Each operator instance:
		- Implements **iterator interface**
		- Efficiently executes operator logic forwarding tuples to next operator
```mermaid
graph BT
    %% Database Tables
    boats[("Boats")]
    reserves[("Reserves")]
    sailors[("Sailors")]

    %% Scan Iterators
    scan_boats["B+Tree<br>Indexed<br>Scan Iterator"]
    scan_reserves["B+Tree<br>Indexed<br>Scan Iterator"]
    scan_sailors["B+Tree<br>Indexed<br>Scan Iterator"]

    %% Select & Project Operators
    select_op["On-the-fly<br>Select Iterator"]
    project_bid["On-the-fly<br>Project Iterator"]
    project_sid["On-the-fly<br>Project Iterator"]
    project_sname["On-the-fly<br>Project Iterator"]

    %% Join Operators
    join1["Index NL Join<br>Iterator"]
    join2["Index NL Join<br>Iterator"]

    %% Define connections from bottom to top
    boats --> scan_boats
    scan_boats -->|"<tt>σ</tt> <sub>color='red'</sub>"| select_op
    select_op -->|"<tt>π</tt> <sub>bid</sub>"| project_bid

    reserves --> scan_reserves

    project_bid --> join1
    scan_reserves --> join1

    join1 -->|"<tt>π</tt> <sub>sid</sub>"| project_sid

    sailors --> scan_sailors

    project_sid --> join2
    scan_sailors --> join2

    join2 -->|"<tt>π</tt> <sub>sname</sub>"| project_sname

    %% Styling
    style boats fill:#fff,stroke:#333,stroke-width:2px
    style reserves fill:#fff,stroke:#333,stroke-width:2px
    style sailors fill:#fff,stroke:#333,stroke-width:2px
    classDef operator fill:#eaf,stroke:#96c,stroke-width:2px;
    class scan_boats,scan_reserves,scan_sailors,select_op,project_bid,project_sid,project_sname,join1,join2 operator;
```

#CS/DB/DBMS/query-plan #CS/DB/DBMS/query-plan/operator


#CS/Courses/CS188 
>[[L12. Iterators, Relational Operators and Joins]]