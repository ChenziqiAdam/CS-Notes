- Arithmetic Expression
	```sql
	SELECT S.age, S.age-5 AS agel, 2*S.age AS age2
	FROM Sailors AS S
	WHERE S.sname = 'Popeye'
```
	```sql
	SELECT S1.sname AS name1, S2.sname AS name2
	FROM Sailors AS S1, Sailors AS S2
	WHERE 2*S1. rating = S2.rating-1
```
	- SQL Calculator!
	```sql
	SELECT
		log(1000) as three,
		exp(ln(2)) as two,
		cos(0) as one,
		ln(2*3) = In(2) + In(3) as sanity;
```
- String Comparisons: Regular Expressions
	```sql
	SELECT S. sname
	FROM Sailors S
	WHERE S.sname LIKE 'B.*'
```
	- This looks for any name starting with a 'B'.

#CS/SQL #CS/regexes 


#CS/Courses/CS186 
>[[L3. SQL II]]