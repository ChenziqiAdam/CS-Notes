- Views
	```sql
	CREATE VIEW view_name
	AS select_statement
```
	- Makes development simpler
	- Often used for security: only have access to the view rather than the table
	- Not "materialized": will not stored in the db
	```sql
	CREATE VIEW Redcount
	AS SELECT B.bid, COUNT (*) AS scount
	FROM Boats2 B, Reserves2 R
	WHERE R.bid=B.bid AND B.color='red'
	GROUP BY B.bid

	SELECT * FROM Redcount
```
- Subquery in FROM: "view on the fly"
	```sql
	SELECT bname, scount
	FROM Boats2 B,
		(SELECT B.bid, COUNT (*)
			FROM Boats2 B, Reserves2 R
			WHERE R.bid = B.bid AND B.color = 'red'
			GROUP BY B.bid) AS Reds(bid, scount)
	WHERE Reds.bid = B.bid
	AND scount < 10
```
- WITH a.k.a. CTE (common table expression)
	- another "view on the fly"
	- can have many queries in WITH
	```sql
	WITH Reds(bid, scount) AS
		(SELECT B. bid, COUNT (*)
		FROM Boats2 B, Reserves2 R
		WHERE R.bid = B.bid AND B.color = 'red'
		GROUP BY B.bid)

	SELECT bname, scount
	FROM Boats2 B, Reds
	WHERE Reds.bid = B.bid
	AND scount < 10
```
- Example: ARGMAX GROUP BY
	```sql
	WITH maxratings(age, maxrating) AS
	(SELECT age, max(rating)
	FROM Sailors
	GROUP BY age)
	
	SELECT S.*
	FROM Sailors S, maxratings m
	WHERE S.age = m.age
	AND S.rating = m.maxrating;
```

#CS/SQL/named-query 


#CS/Courses/CS186 
>[[L3. SQL II]]