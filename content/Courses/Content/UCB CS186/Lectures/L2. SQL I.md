- [[L2.1 SQL basic]]
- [[L2.2 SQL syntax by example]]
- Summary
	```sql
	SELECT [DISTINCT] <column expression list>
		FROM <single table>
		[WHERE <predicate>]
		[
		GROUP BY <column list>
		[HAVING <predicate>] 
		]
		[ORDER BY <column list>]
		[LIMIT <integer>];
```
	- Relational model has well-defined query semantics
	- Modern SQL extends "pure" relational model (duplicate rows, aggregates, ordering of output)
	- Typically, many ways to write a query
		- DBMS figures out a fast way to execute a query, regardless of how it is written.

#CS/SQL 


#CS/Courses/CS186 
>[[UCB CS186]]