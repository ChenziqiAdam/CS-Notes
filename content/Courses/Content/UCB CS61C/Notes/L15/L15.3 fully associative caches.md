- ***Fully Associative Caches*** (a large "bucket"): Each memory block can map *anywhere* in the cache (*fully associative*)
	- Most efficient use of space
	- Least efficient to check
- To check a fully associative cache:
	1) Look at ALL cache slots in sequence
	2) If Valid bit is `0`, then ignore
	3) If Valid bit is `1` and *Tag* matches, then return that data
	- Note: Cache must store valid and tag bits!
	- (In *FA cache*, *Tag* is unique to entire cache since 1 large "bucket", not the case in other designs)
- Memory Address Fields:
	![[Pasted image 20250329110238.png]]
	- Meaning of the field sizes:
		- Offset bits <-> 2<sup>Offset</sup> bytes per block = 2<sup>Offset-2</sup> words per block
		- Tag bits = A - Offset, where A = # of address bits (A = 32 here)
- Exercise
	![[Pasted image 20250329110642.png]]

#CS/HW/memory/cache/fully-associative-cache


#CS/Courses/CS61c 
>[[L15. Memory Hierarchy and Fully Associative Caches]]