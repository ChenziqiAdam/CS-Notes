1. Get the instruction
	![[Pasted image 20250313111709.png]]
2. Parse instruction fields (`rd`, `rs1`, `rs2`, operation...)
	![[Pasted image 20250313111817.png]]
3. Read data based on parsed operands
	![[Pasted image 20250313111847.png]]
- Storage Element: Register File
	![[Pasted image 20250313111926.png]]
4. Perform operation
	![[Pasted image 20250313111952.png]]
- Q. How to tell which R-type operation to execute?
	- ALU == "Arithmetic Logic Unit"
	- `ALUSel` is a control bit which encodes the operation we should perform on the given operands
		- The value of `ALUSel` is a mapping from `func3` and `func7` values to operations
		- e.g. if `func3` == `000` and `func7` == `0000000`, perform an `add`
		- Multiple `func3`, `func7` combinations might lead to the same operation! (ie. `add` and `addi`)
5. Write result to the destination register
	![[Pasted image 20250313112217.png]]

#CS/HW/CPU/datapath/R-type


#CS/Courses/CS61c 
>[[L12.2 build the datapath]]