### Arithmetic Instructions Multiply Extension
- Multiplication (`mul` and `mulh`)
	- `mul dst, srcl, src2`
	- `mulh dst, srcl, src2`
	- `src1*src2`: lower 32-bits through `mul`, upper 32- bits in `mulh`
- Division (`div`)
	- `div dst, src1, src2`
	- `rem dst, src1, src2`
	- `src1/src2`: quotient via `div`, remainder via `rem`

### Compare Instructions
- Set Less Than (`slt`)
	- `slt dst, regl, reg2`
	- If value in `reg1` < value in `reg2`, `dst` = 1, else 0
- Set Less Than Immediate (`slti`)
	- `slti dst, reg1, imm`
	- If value in `reg1` < `imm`, `dst` = 1, else 0
- Example: Another if else design
	```
	# i+s0, j+s1
	# a-s2, b+s3
	slt t0 s0 s1
	beg to,x0, else
	then:
	add s2, s3, x0
	end
	else:
	sub s2, x0, s3
	end:
	```

### Environment Call
- `ecall` is a way for an application to interact with the operating system
- The value in register a0 is given to the OS which performs special functions
	- Printing values
	- Exiting the program
	- Allocating more memory for the program

#CS/RICS-V/ecall 


#CS/Courses/CS61c 
>[[L6.3 RISC-V instructions]]