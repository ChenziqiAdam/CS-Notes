- Stack pointer (`sp`) holds the address of the bottom of the stack
	- Decrement it (recall stack grows downwards)
	- Then use `sw` (store word) to write to a variable
	- To "clean up", just increment the stack pointer
- Example
	```
	# store tO to the stack
	addi sp, sp, -4   # stack grows downwards
	sw to, 0(sp)
	```

#CS/RICS-V/stack


#CS/Courses/CS61c 
[[L7.4 functions in Assembly]]